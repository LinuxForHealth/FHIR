<server description="fhir-server">

    <!-- Enable features -->
    <featureManager>
        <feature>localConnector-1.0</feature>
        <feature>jaxrs-2.0</feature>
        <feature>jpa-2.1</feature>
        <feature>appSecurity-2.0</feature>
        <feature>ssl-1.0</feature>
        <feature>cdi-1.2</feature>
        <feature>jsonp-1.0</feature>
        <feature>beanValidation-1.1</feature>
        <feature>jsf-2.2</feature>
    </featureManager>

    <!-- This template enables security. To get the full use of all the capabilities, a keystore and user registry are required. -->
    
    <!-- For the keystore, default keys are generated and stored in a keystore. To provide the keystore password, generate an 
         encoded password using bin/securityUtility encode and add it below in the password attribute of the keyStore element. 
         Then uncomment the keyStore element. -->
CODE_REMOVED
    
    <!--For a user registry configuration, configure your user registry. For example, configure a basic user registry using the
        basicRegistry element. Specify your own user name below in the name attribute of the user element. For the password, 
        generate an encoded password using bin/securityUtility encode and add it in the password attribute of the user element. 
        Then uncomment the user element. -->
    <basicRegistry id="basic" realm="BasicRealm"> 
CODE_REMOVED
    </basicRegistry>
    
    <!-- To access this server from a remote client add a host attribute to the following element, e.g. host="*" -->
    <httpEndpoint httpPort="9080" httpsPort="9443" id="defaultHttpEndpoint"/>
        
    <library id="derbyLib">
        <fileset dir="${shared.resource.dir}/lib/derby" includes="*.jar"/>
    </library>

    <library apiTypeVisibility="spec,third-party" id="moxyLib">
        <fileset dir="${shared.resource.dir}/lib/moxy" includes="*.jar"/>
    </library>
    
    <dataSource id="derbyDB" jndiName="jdbc/fhirDerbyDB">
        <jdbcDriver libraryRef="derbyLib"/>
CODE_REMOVED
    </dataSource>
    
    <dataSource id="derbyDBNonJTA" jndiName="jdbc/fhirDerbyDBNonJTA" transactional="false">
        <jdbcDriver libraryRef="derbyLib"/>
CODE_REMOVED
    </dataSource>
    
    <!-- This required JNDI entry specifies the name of the FHIRPersistence implementation class to be used by the server -->
    <jndiEntry value="com.ibm.watsonhealth.fhir.persistence.jpa.impl.FHIRPersistenceJPAImpl" jndiName="com.ibm.watsonhealth.fhir.persistence.FHIRPersistence.classname" id="fhirPersistenceImpl"/>

    <!-- Automatically expand WAR files and EAR files -->
    <applicationManager autoExpand="true"/>

    <applicationMonitor updateTrigger="mbean"/>   

    <webApplication id="fhir-server" location="fhir-server.war" name="fhir-server">
        <classloader apiTypeVisibility="spec,third-party" commonLibraryRef="moxyLib"/>
    </webApplication>

    <jpa defaultPersistenceProvider="org.eclipse.persistence.jpa.PersistenceProvider"/>

    <!-- 
        Modify the trace string below as needed to enable/disable tracing. 
        Hint: com.ibm.watsonhealth.fhir.*=finer 
    -->
    <logging traceSpecification="*=info" traceFormat="BASIC"/>
</server>
